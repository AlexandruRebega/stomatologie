RULARE:
docker-compose build --no-cache
docker-compose up
docker run mysql -P 3306

STERGERE IMAGINI:
docker-compose down
docker system prune -a

CONECTARE MYSQL:
docker run --env="MYSQL_ROOT_PASSWORD=root" -p 32000:3306 mysql
mysql -P 32000 -u root -p dent
